<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <group
        android:id="@+id/menu_group_remove_from_favorites"
        android:visible="false">
        <item
            android:id="@+id/menu_item_remove_from_favorites"
            android:icon="@drawable/ic_oui_favorite_on"
            android:title="@string/remove_from_fav"
            app:showAsAction="always"
            tools:ignore="AlwaysShowAction,AppCompatResource" />
    </group>
    <group
        android:id="@+id/menu_group_add_to_favorites"
        android:visible="true">
        <item
            android:id="@+id/menu_item_add_to_favorites"
            android:icon="@drawable/ic_oui_favorite_off"
            android:title="@string/add_to_fav"
            app:showAsAction="always"
            tools:ignore="AppCompatResource" />
    </group>
</menu>